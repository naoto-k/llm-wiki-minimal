# 2026-05-20

## Session: myproject_abc123

- 判断: API エラー時の retry は exponential backoff + jitter を採用。即時 retry はサーバー側に過負荷を与えるため見送り
- 変更: `src/client.ts` に `retryWithBackoff` を追加、HTTP 5xx と 429 のみ対象に
- 残課題: timeout 値の調整、circuit breaker パターン導入の要否を次回検討

## Session: myproject_def456

- 判断: try/except で例外を握りつぶさず、必ずログ出力 + 上位への再送出をルール化
- 変更: `src/db.ts` の 3 箇所で例外を握りつぶしていたのを修正、`logger.error` + `throw` に統一
- 残課題: 既存コードに同パターンが他に無いか、grep で全件確認

## 昇格候補

- エラーハンドリングの設計原則（try/except 握りつぶし禁止、retry の戦略）
